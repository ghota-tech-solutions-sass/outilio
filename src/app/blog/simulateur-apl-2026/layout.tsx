import { articleMetadata } from "../_data/metadata";

export const metadata = articleMetadata("simulateur-apl-2026");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
